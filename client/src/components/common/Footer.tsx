type FooterProps = {
  // for future use if needed
}

const Footer = (_: FooterProps) => {
  return (
    <footer>
      <div>
		<h1> this footer </h1>
	  </div>
    </footer>
  )
}

export default Footer
